-- Database: public

-- DROP DATABASE IF EXISTS public;

-- CREATE DATABASE public
--     WITH
--     OWNER = postgres
--     ENCODING = 'UTF8'
--     LC_COLLATE = 'English_United States.1254'
--     LC_CTYPE = 'English_United States.1254'
--     TABLESPACE = pg_default
--     CONNECTION LIMIT = -1
--     IS_TEMPLATE = False;

-- Use SQL to get the following from the database:
-- All items, ordered by price (lowest to highest).
-- Items with a price above 80 (80 included), ordered by price (highest to lowest).
-- The first 3 customers in alphabetical order of the first name (A-Z) – exclude the primary key column from the results.
-- All last names (no other columns!), in reverse alphabetical order (Z-A)
select * from items ORDER BY item_price ASC ;
select item_name , item_price from items WHERE item_price>=80 ORDER BY item_price ASC ; /* highest to lowest DESC */
SELECT customer_name , customer_lastname FROM customers ORDER BY customer_name ASC LIMIT 3 
SELECT customer_lastname FROM customers ORDER BY customer_lastname DESC


